## Apps

Workloads in this folder are deployed by ArgoCD Applications.

Current app manifests:

- `apps/test-app/` — simple workload for GitOps self-heal demo
- `apps/ai-model-alpha/` — sample KServe `InferenceService`
